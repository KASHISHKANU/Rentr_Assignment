import 'package:flutter/material.dart';

class AgentDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Agent Dashboard")),
      body: Center(child: Text("Agent Dashboard Screen")),
    );
  }
}
